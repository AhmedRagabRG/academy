# Admissions route boundary

Pages in this segment remain Server Components. Dynamic pages await the Next.js 16 promise-based `params` prop and pass serializable identifiers into interactive Admissions screens. Data, fixtures, permissions, and business rules are owned by the feature service boundary.
