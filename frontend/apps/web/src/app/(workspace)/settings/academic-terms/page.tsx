import type { Metadata } from "next"
import { AcademicTermsScreen } from "@/features/organization-settings"
export const metadata: Metadata = { title: "الفصول الأكاديمية" }
export default function Page() { return <AcademicTermsScreen /> }
