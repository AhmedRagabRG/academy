import { FilterBar } from "@/shared/components/data-table/filter-bar"
export function AdministrativeTableToolbar({ children }: { children: React.ReactNode }) { return <FilterBar>{children}</FilterBar> }
