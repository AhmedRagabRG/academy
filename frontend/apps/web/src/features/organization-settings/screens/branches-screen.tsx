"use client"
import type { ColumnDef } from "@tanstack/react-table"
import { Button } from "@workspace/ui/components/button"
import { useState } from "react"
import { StatusBadge } from "@/shared/components/feedback/status-badge"
import { EntityManager } from "../components/entity-manager"
import { BranchForm } from "../forms/branch-form"
import { useEntityList, useEntityMutations } from "../hooks/use-entity-management"
import type { Branch } from "../types/domain"
import { defaultListQuery } from "../utils/list-query-state"
export function BranchesScreen() { const [query, setQuery] = useState(defaultListQuery); const [editing, setEditing] = useState<Branch | "new" | null>(null); const list = useEntityList("branches", query); const mutations = useEntityMutations("branches"); const columns: ColumnDef<Branch>[] = [{ accessorKey: "name", header: "الفرع" }, { accessorKey: "code", header: "الرمز" }, { accessorKey: "address", header: "العنوان" }, { accessorKey: "status", header: "الحالة", cell: ({ row }) => <StatusBadge label={row.original.status === "active" ? "نشط" : "مؤرشف"} tone={row.original.status === "active" ? "success" : "neutral"} /> }, { id: "actions", header: "الإجراءات", cell: ({ row }) => <div className="flex gap-2"><Button variant="ghost" onClick={() => setEditing(row.original)}>تعديل</Button><Button variant="ghost" onClick={() => mutations.status.mutate({ id: row.original.id, status: row.original.status === "active" ? "archived" : "active", version: row.original.version })}>{row.original.status === "active" ? "أرشفة" : "تفعيل"}</Button></div> }]; return <EntityManager title="الفروع" description="إدارة مواقع المؤسسة وبيانات الاتصال وساعات العمل دون حذف السجل التاريخي." records={list.data} query={query} loading={list.isLoading} error={list.error} columns={columns} editing={Boolean(editing)} onCreate={() => setEditing("new")} onClose={() => setEditing(null)} onSearch={(search) => setQuery((current) => ({ ...current, search, page: 1 }))} onPage={(page) => setQuery((current) => ({ ...current, page }))} refetch={() => void list.refetch()} editor={<BranchForm record={editing === "new" ? undefined : editing ?? undefined} pending={mutations.create.isPending || mutations.update.isPending} error={mutations.create.error ?? mutations.update.error} onSubmit={(values) => editing === "new" ? mutations.create.mutate(values) : editing && mutations.update.mutate({ id: editing.id, input: { ...values, expectedVersion: editing.version } })} />} /> }
