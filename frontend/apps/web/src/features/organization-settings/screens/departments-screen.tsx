"use client"
import type { ColumnDef } from "@tanstack/react-table"
import { Button } from "@workspace/ui/components/button"
import { useState } from "react"
import { StatusBadge } from "@/shared/components/feedback/status-badge"
import { EntityManager } from "../components/entity-manager"
import { DepartmentForm } from "../forms/department-form"
import { useEntityList, useEntityMutations } from "../hooks/use-entity-management"
import type { Department } from "../types/domain"
import { defaultListQuery } from "../utils/list-query-state"
export function DepartmentsScreen() { const [query, setQuery] = useState(defaultListQuery); const [editing, setEditing] = useState<Department | "new" | null>(null); const list = useEntityList("departments", query); const mutations = useEntityMutations("departments"); const columns: ColumnDef<Department>[] = [{ accessorKey: "name", header: "القسم" }, { accessorKey: "description", header: "الوصف" }, { accessorKey: "status", header: "الحالة", cell: ({ row }) => <StatusBadge label={row.original.status === "active" ? "نشط" : "غير نشط"} tone={row.original.status === "active" ? "success" : "warning"} /> }, { id: "actions", header: "الإجراءات", cell: ({ row }) => <div className="flex gap-2"><Button variant="ghost" onClick={() => setEditing(row.original)}>تعديل</Button><Button variant="ghost" onClick={() => mutations.status.mutate({ id: row.original.id, status: row.original.status === "active" ? "inactive" : "active", version: row.original.version })}>{row.original.status === "active" ? "تعطيل" : "تفعيل"}</Button></div> }]; return <EntityManager title="الأقسام" description="هيكل إداري قابل للتهيئة تستخدمه حسابات الموظفين والوحدات المستقبلية." records={list.data} query={query} loading={list.isLoading} error={list.error} columns={columns} editing={Boolean(editing)} onCreate={() => setEditing("new")} onClose={() => setEditing(null)} onSearch={(search) => setQuery((current) => ({ ...current, search, page: 1 }))} onPage={(page) => setQuery((current) => ({ ...current, page }))} refetch={() => void list.refetch()} editor={<DepartmentForm record={editing === "new" ? undefined : editing ?? undefined} pending={mutations.create.isPending || mutations.update.isPending} error={mutations.create.error ?? mutations.update.error} onSubmit={(values) => editing === "new" ? mutations.create.mutate(values) : editing && mutations.update.mutate({ id: editing.id, input: { ...values, expectedVersion: editing.version } })} />} /> }
