"use client"

/**
 * The shared summary under this module's name.
 *
 * The behaviour is identical in every editor, so it lives in `shared` and this
 * stays the module's entry point rather than a second copy that can drift.
 */
export { FormErrorSummary as StudentFormErrorSummary } from "@/shared/components/forms/form-error-summary"
