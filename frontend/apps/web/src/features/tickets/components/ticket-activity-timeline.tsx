import { Clock3 } from "lucide-react"
import type { TicketActivity } from "../types/domain"
export function TicketActivityTimeline({ activity }: { activity: TicketActivity[] }) { return <section aria-labelledby="activity-title"><h2 id="activity-title" className="mb-4 text-lg font-bold">سجل النشاط</h2><ol className="border-border ms-2 space-y-5 border-s ps-5">{[...activity].reverse().map((item) => <li key={item.id} className="relative"><span className="bg-primary absolute -start-[1.65rem] top-1 size-2.5 rounded-full" /><p className="font-medium">{item.message}</p><p className="text-muted-foreground mt-1 flex items-center gap-1 text-xs"><Clock3 className="size-3" />{item.actorName} · <bdi dir="ltr">{new Intl.DateTimeFormat("ar-EG", { dateStyle: "medium", timeStyle: "short" }).format(new Date(item.occurredAt))}</bdi></p></li>)}</ol></section> }
