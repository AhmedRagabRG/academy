"use client"
import { FileText, ImageIcon, Upload } from "lucide-react"
import type { TicketAttachment } from "../types/domain"
export function TicketAttachmentsPanel({ attachments, pending, onUpload }: { attachments: TicketAttachment[]; pending?: boolean; onUpload: (file: File) => void }) { return <section><h2 className="mb-3 text-lg font-medium">المرفقات</h2><label className="border-border hover:bg-muted/30 flex cursor-pointer items-center justify-center gap-2 rounded-lg border border-dashed p-4 text-sm"><Upload className="size-4" />رفع صورة أو PDF أو مستند<input className="sr-only" type="file" accept="image/*,.pdf,.doc,.docx" disabled={pending} onChange={(e) => { const file = e.target.files?.[0]; if (file) onUpload(file) }} /></label><div className="mt-3 space-y-2">{attachments.map((file) => <div key={file.id} className="bg-muted/40 flex items-center gap-3 rounded-lg p-3">{file.mimeType.startsWith("image/") ? <ImageIcon /> : <FileText />}<div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{file.name}</p><p className="text-muted-foreground text-xs"><bdi dir="ltr">{Math.ceil(file.sizeBytes / 1024)} KB</bdi></p></div>{file.previewUrl && <a className="border-border rounded-md border px-2 py-1 text-xs" href={file.previewUrl} target="_blank" rel="noreferrer">معاينة</a>}</div>)}</div></section> }
