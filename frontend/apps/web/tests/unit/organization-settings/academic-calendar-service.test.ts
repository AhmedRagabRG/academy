import { beforeEach, describe, expect, it } from "vitest"
import { mockOrganizationSettingsService as service } from "@/features/organization-settings/services/mock-organization-settings-service"
import { mockScenarioController } from "@/features/organization-settings/services/mock-scenario-controller"
describe("academic calendar service", () => { beforeEach(() => { service.reset(); mockScenarioController.setLatency(0) }); it("activates exactly one year and updates the default", async () => { const previous = await service.get("academic-years", "year-2025"); await service.activateAcademicYear(previous.id, previous.version); const years = await service.list("academic-years", { page: 1, pageSize: 10, status: "all" }); expect(years.items.filter((year) => year.status === "active")).toHaveLength(1); expect((await service.getGeneralSettings()).defaultAcademicYearId).toBe(previous.id) }); it("rejects terms outside the parent year", async () => { await expect(service.create("academic-terms", { academicYearId: "year-2026", academicYearName: "2026", name: "خارج النطاق", order: 3, startDate: "2028-01-01", endDate: "2028-02-01", status: "inactive" })).rejects.toMatchObject({ code: "term-outside-year" }) }) })
